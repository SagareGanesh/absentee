/*
 * StudentComponent Messages
 *
 * This contains all the text for the StudentComponent component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  StudentComponent: {
    id: 'StudentComponent.component.message',
    defaultMessage: 'StudentComponent',
  },
});
