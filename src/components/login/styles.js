const absolute = {
  position: 'absolute',
  width: '100%',
  height: '100%',
  filter: "blur(5px)",
};

const image = {
  width: '100%',
  height: '100%',
};

export default {
  absolute,
  image,
};
