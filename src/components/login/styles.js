const absolute = {
  position: 'absolute',
  width: '100%',
  height: '100%',
  filter: "blur(5px)",
};

const image = {
  width: '100%',
  height: '100%',
};

const marginAuto = {
  margin: 'auto',
};

export default {
  absolute,
  image,
  marginAuto,
};
